type Image {
    "The image's unique identifier"
    id: ID
    "Url of the image"
    url: String!
    "The image's title"
    image_name: String
    "Created day"
    createdDay: DateTime
}

input ImageInput {
    "The image's unique identifier"
    id: ID
    "Url of the image"
    url: String!
    "The image's title"
    image_name: String
}
