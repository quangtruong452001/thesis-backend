

interface MutationOf {
    "Mutation status"
    success: Boolean
    "Mutation message"
    msg: String
}
interface Paginator {
    hasNextPage: Boolean
    limit: Int
    page: Int
    prevPage: Int
    total: Int
    hasPrevPage: Boolean
    nextPage: Int
    pagingCounter: Int
    totalPages: Int
}

type Hour {
    _id: ID
    name: String
    time: Int
    timeFrame: String
    slot: Int
}

